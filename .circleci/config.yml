version: 2

jobs:
	build:
		docker:
			# specify the version you desire here
			- image: circleci/python:3.6.1
	## Running Tests
	steps:
      - checkout
	  - run:
			command: |
			sudo pip install -U selenium
      - run: sh SauceLabs.sh